/**
 * Tipos de domínio compartilhados do frontend.
 *
 * Placeholder de fundação (Story 1.2). Os tipos reais (Caixinha,
 * Participante, Estado da máquina financeira, etc.) entram nos épicos que
 * consumirem a API — devem casar 1:1 em camelCase com o contrato do backend
 * (architecture#Naming Patterns). Dinheiro sempre como string na borda da
 * API (ver `@/lib/money`), nunca `number`.
 */

export {};
