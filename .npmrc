# Este projeto usa pnpm (ver "packageManager" no package.json e o CI).
# engine-strict faz `npm install` falhar cedo com mensagem clara em vez de
# corromper o node_modules — use `pnpm install`.
engine-strict=true
